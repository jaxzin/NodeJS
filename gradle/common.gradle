project.ext {
	nodeHome	= "$rootProject.buildDir/node-home"
	nodeGroupId = 'org.nodejs'
	nodeName	= "node"
	nodeVersion	= "0.8.20"
	nodeExt		='tar.gz'
	localRepo 	= "file://$rootProject.buildDir/local-repo"
}
